@{
    ViewData["Title"] = "Quản lý thông báo hệ thống";
    Layout = "~/Views/Shared/Superadmin_Layout.cshtml";
}

<div class="history-container">
    <div class="history-table-container">
        <!-- Components table  -->
           @await Component.InvokeAsync("Table", new { 
            columnNames = new List<string> { "STT", "Title", "Priority","Date",
                "Status","Action"
                }, 
            tableBodyId = "staffBody" 
        })
    </div>

    @await Component.InvokeAsync("Pagination", new Components.Model.Pagination.PaginationComponentModel {
        CurrentPage = 1,
        TotalPages = 1,
        TotalItems = 1,
        ItemsPerPage = 15,
        PageAction = "staffPageChange"
    })
</div>

@section Scripts {
    <script src="~/js/logic/noti.js"></script>
}

@{
    ViewData["Title"] = "Setting";
    Layout = "~/Views/Shared/Main_Layout.cshtml";
}

<style>
    .setting-container {
        @* max-width: 800px; *@
        margin: 0 auto;
        padding: 32px;
        background-color: #fff;
    }

    .setting-title {
        font-size: 24px;
        font-weight: 700;
        color: #4A4A4A;
        margin-bottom: 24px;
    }

    .warning-text {
        color: #EB455F;
        font-size: 18px;
        margin-bottom: 24px;
    }

    .section-title {
        font-size: 18px;
        font-weight: 700;
        color: #4A4A4A;
        margin-bottom: 16px;
    }

    .instruction-list {
        list-style: none;
        display: flex;
        flex-direction: column;
        padding-left: 0;
        margin-bottom: 24px;
        gap: 12px;
    }

    .instruction-list li {
        font-size: 16px;
        color: #4A4A4A;
        margin-bottom: 8px;
        font-weight: 700;
    }

    .form-group label {
        font-size: 14px;
        color: #555;
        margin-bottom: 8px;
        display: block;
    }

    .port-input-group {
        display: flex;
        gap: 16px;
        margin-bottom: 32px;
        align-items: flex-end;
    }

    .port-input {
        flex-grow: 1;
        padding: 10px 12px;
        border: 1px solid #ddd;
        border-radius: 4px;
        font-size: 16px;
        color: #333;
    }

    .port-input::placeholder {
        color: #aaa;
    }

    .update-button {
        padding: 10px 46px;
        background-color: #EB455F;
        color: #fff;
        border: none;
        border-radius: 6px;
        font-size: 16px;
        font-weight: 700;
        cursor: pointer;
        transition: background-color 0.2s;
    }

    .update-button:hover {
        background-color: #c82333;
    }

    .purpose-list {
        list-style-type: disc;
        padding-left: 20px;
        margin-bottom: 32px;
    }

    .purpose-list li {
        font-size: 14px;
        color: #555;
        margin-bottom: 8px;
    }

    .contact-list {
        list-style: none;
        padding-left: 0;
    }

    .contact-list li {
        font-size: 14px;
        color: #4A4A4A;
        margin-bottom: 8px;
    }

    .contact-list li strong {
        font-weight: 700;
        color: #4A4A4A;
    }
</style>

<div class="setting-container">
    <h1 class="setting-title">Cài đặt IP public WI-FI cho cửa hàng</h1>
    <p class="warning-text">Lưu ý: Nếu IP public có thay đổi hãy cập nhật, 
        nếu IP public sai hoặc chưa có sẽ không truy cập được vào web khách hàng.</p>

    <div id="ip-warning" style="display:none; font-size:14px; margin-bottom: 8px; text-align:center;"></div>
    
    <div class="form-group" style="display: flex; align-items: center; gap: 8px; margin-bottom: 12px;">
        <label for="currentIp" style="margin-bottom: 0; font-size: 14px;">IP hiện tại của nhà hàng:</label>
        <div id="currentIp" style="color: #0c6e35; font-size: 14px; margin-bottom: 0;">Đang tải...</div>
    </div>
  
    <div class="form-group">
        <label for="portIp">Port IP thay đổi mới nhất:</label>
        <div class="port-input-group">
            <input type="text" id="portIp" class="port-input" placeholder="VD: 10.21.9.223">
            <button class="update-button">Cập nhật Port</button>
        </div>
    </div>

    <h2 class="section-title">Mục đích cài đặt Port:</h2>
    <ul class="purpose-list">
        <li>Bảo mật: Ngăn chặn người lạ từ bên ngoài truy cập trái phép, đặt giả đơn hoặc làm rối hệ thống.</li>
        <li>Kiểm soát: Chỉ thiết bị bị trong cửa hàng mới được truy cập, giúp dễ để quản lý và hạn chế lỗi.</li>
    </ul>

    <h2 class="section-title">Liên hệ hỗ trợ:</h2>
    <ul class="contact-list">
        <li><strong>Hotline:</strong> 0916070054 (Hỗ trợ 24/7)</li>
        <li><strong>Gmail:</strong> ductri09072004@gmail.com</li>
    </ul>
</div>

@section Scripts {
    <script src="~/js/config/api.config.js"></script>
    <script src="~/js/api_service/restaurant.service.js"></script>
    <script src="~/js/logic/setting.js"></script>
}

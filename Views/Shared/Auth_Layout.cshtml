        <!DOCTYPE html>
        <html lang="vi">
        <head>
            <meta charset="utf-8" />
            <title>@ViewData["Title"] - JolliServe</title>
            <meta name="viewport" content="width=device-width, initial-scale=1" />
            <link rel="icon" type="image/x-icon" href="~/logo_web_admin.ico" />
            <link rel="icon" type="image/png" sizes="32x32" href="~/png/logo_web_admin.png" />
            <link rel="apple-touch-icon" href="~/png/logo_web_admin.png" />
            <link rel="stylesheet" href="~/css/site.css" />
        <!-- Không thay đổi phần <head> -->
        <style>
            /* Ẩn thanh cuộn cho tất cả các trình duyệt */
            ::-webkit-scrollbar {
                width: 0;
                background: transparent;
            }

            /* Ẩn thanh cuộn cho Firefox */
            * {
                scrollbar-width: none;
            }

            /* Ẩn thanh cuộn cho IE và Edge */
            * {
                -ms-overflow-style: none;
            }

            body, html {
                height: 100%;
                margin: 0;
                font-family: 'Segoe UI', sans-serif;
                overflow-x: hidden;
                overflow-y: auto; /* Thay đổi từ hidden thành auto */
            }

            .login-layout {
                box-sizing: border-box;
                position: relative;
                width: 100%;
                min-height: 100vh;
               background: url('/jpg/background.jpg') no-repeat center center;
                background-size: cover;
                display: flex;
                align-items: center;
                justify-content: center;
                overflow: hidden;
                padding: 24px;
                flex-wrap: wrap;
                gap: 40px;
            }

            .welcome-section {
                position: relative;
                color: #333;
                max-width: 50%;
                padding: 24px;
                min-width: 300px;
            }

            .welcome-section h1 {
                font-size: clamp(24px, 4vw, 36px);
                font-weight: bold;
                margin-bottom: 15px;
                text-align: center;
            }

            .welcome-section p {
                font-size: clamp(14px, 2vw, 16px);
                margin-top: 15px;
                line-height: 1.5;
                text-align: center;
            }

            .welcome-section img {
                max-width: 100%;
                height: auto;
                display: block;
                margin: 0 auto; 
            }

            .support {
                display: flex;
                justify-content: center;
                font-size: 14px;
                color: red;
                margin-top: 20px;
            }


            .login-form-wrapper {
                background-color: #fff;
                border-radius: 32px;
                box-shadow: 0 0 24px #4A4A4A;
                padding: clamp(20px, 4vw, 32px);
                width: min(420px, 100%);
                max-width: 35%;
                min-height: auto;
                z-index: 1;
                box-sizing: border-box;
                max-height: 100vh;
                display: flex;
                flex-direction: column;
                min-width: 300px;
                overflow-y: auto;
            }

            @@media (max-width: 1024px) {
                .login-layout {
                    justify-content: center;
                    padding: 15px;
                    gap: 30px;
                }

                .welcome-section {
                    max-width: 100%;
                    text-align: center;
                    margin-bottom: 0;
                }

                .login-form-wrapper {
                    max-width: 90%;
                    margin: 0;
                    max-height: 80vh;
                    min-height: auto;
                }

                .support {
                    position: relative;
                    bottom: auto;
                    left: auto;
                    text-align: center;
                    margin-top: 20px;
                    width: 100%;
                }
            }

            @@media (max-width: 768px) {
                .login-layout {
                    padding: 10px;
                    gap: 20px;
                }

                .welcome-section h1 {
                    font-size: 24px;
                }

                .welcome-section p {
                    font-size: 14px;
                }

                .login-form-wrapper {
                    padding: 20px;
                    max-height: 70vh;
                    min-height: auto;
                }
            }

            @@media (max-width: 480px) {
                .login-layout {
                    padding: 5px;
                    gap: 15px;
                }

                .welcome-section {
                    padding: 10px;
                }

                .login-form-wrapper {
                    padding: 15px;
                    max-height: 60vh;
                    min-height: auto;
                }
            }
        </style>

        </head>
        <body>
            <div class="login-layout">
                <!-- Welcome text and background -->
                <div class="welcome-section">
                    <img src="~/svg/sidebar/cart.svg" alt="Login Illustration" />
                    <p style="font-size: 32px; font-weight: 700; text-align: center;">
                         Chào mừng đến với Hệ Thống Nhà Hàng
                    </p>

                    <p  style="font-size: 16px; font-weight: 400;">
                        Hệ thống quản lý nhà hàng toàn diện giúp bạn theo dõi, điều phối và quản lý hoạt động kinh doanh một cách hiệu quả.<br />
                        Đăng nhập để bắt đầu công việc của bạn ngay hôm nay!
                    </p>
                    <div class="support">
                    Hỗ trợ kỹ thuật: 0389105492 (A. Trí công nghệ)
                    </div>
                </div>

                <!-- Form login đè lên -->
                <div class="login-form-wrapper">
                    @RenderBody()
                </div>              
            </div>
            @await RenderSectionAsync("Scripts", required: false)
        </body>
        </html>
